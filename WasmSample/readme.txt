# How to run the sample .wasm code

Opening the index.html will result in functions.wasm not loading. The .wasm file will only be loaded if the website is run on a server.

Recommended easy to use server (also used in demo): 
https://www.npmjs.com/package/http-server
